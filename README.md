# بوت تليجرام للمصادقة 2FA

## نظرة عامة

هذا البوت مصمم لإرسال رموز مصادقة 2FA من خلال TOTP_SECRET بشكل دوري إلى مجموعات تليجرام خاصة. يتميز البوت بالعديد من الخصائص المتقدمة مثل:

- إرسال رموز المصادقة 2FA بشكل دوري (افتراضياً كل 10 دقائق)
- دعم العمل مع مجموعات متعددة في نفس الوقت
- إدارة متعددة المسؤولين
- واجهة إدارة ديناميكية باستخدام الأزرار
- نظام تتبع محاولات النسخ لكل مستخدم
- تحديث يومي لعدد المحاولات المسموحة
- تخصيص تنسيق الرسائل والمنطقة الزمنية

## المتطلبات

- Python 3.6 أو أحدث
- حساب تليجرام
- بوت تليجرام (يمكن إنشاؤه عبر BotFather)
- مجموعة خاصة على تليجرام
- TOTP_SECRET لتوليد رموز المصادقة

## التثبيت

### 1. تثبيت الحزم المطلوبة

```bash
pip3 install pyotp pytz python-telegram-bot schedule
```

### 2. تنزيل ملفات البوت

يمكنك تنزيل ملفات البوت من مستودع GitHub:

```bash
git clone https://github.com/NaJiKuN/ChatGPTPlus2FABot.git
cd ChatGPTPlus2FABot
```

أو يمكنك نسخ الملفات يدوياً إلى المجلد المطلوب.

### 3. تكوين البوت

افتح ملف `config.py` وتأكد من تعيين رمز البوت (TOKEN) الخاص بك:

```python
BOT_CONFIG = {
    "TOKEN": "8119053401:AAHuqgTkiq6M8rT9VSHYEnIl96BHt9lXIZM",
    "ADMIN_IDS": ["764559466"],  # يمكن إضافة مسؤولين آخرين هنا
    "DEFAULT_INTERVAL": 10,  # الفترة الافتراضية لإرسال الرموز (بالدقائق)
    "DEFAULT_TIMEZONE": "Asia/Jerusalem",  # التوقيت الافتراضي (فلسطين)
    "DEFAULT_TIME_FORMAT": "12",  # صيغة الوقت الافتراضية (12 ساعة)
    "DEFAULT_ATTEMPTS": 3,  # عدد محاولات النسخ الافتراضية لكل مستخدم
}
```

## تشغيل البوت

### تشغيل مباشر

يمكنك تشغيل البوت مباشرة باستخدام الأمر:

```bash
python3 bot.py
```

### تشغيل كخدمة نظام

لتشغيل البوت كخدمة نظام على نظام Linux، اتبع الخطوات التالية:

1. انسخ ملف الخدمة إلى مجلد خدمات النظام:

```bash
sudo cp ChatGPTPlus2FABot.service /etc/systemd/system/
```

2. قم بتحديث خدمات النظام:

```bash
sudo systemctl daemon-reload
```

3. قم بتشغيل الخدمة:

```bash
sudo systemctl start ChatGPTPlus2FABot
```

4. قم بتفعيل الخدمة لتعمل تلقائياً عند إعادة تشغيل النظام:

```bash
sudo systemctl enable ChatGPTPlus2FABot
```

5. للتحقق من حالة الخدمة:

```bash
sudo systemctl status ChatGPTPlus2FABot
```

## استخدام البوت

### الأوامر المتاحة

- `/start`: بدء استخدام البوت
- `/admin`: الوصول إلى لوحة تحكم المسؤول (متاح فقط للمسؤولين)

### إعداد البوت للاستخدام

1. أضف البوت إلى المجموعة الخاصة التي ترغب في استلام رموز المصادقة فيها
2. تأكد من منح البوت صلاحيات الإدارة في المجموعة
3. استخدم أمر `/admin` للوصول إلى لوحة التحكم
4. اختر "إضافة/تعديل مجموعة وإعداد TOTP_SECRET"
5. أدخل معرف المجموعة الخاصة (يمكن الحصول عليه من خلال إضافة @RawDataBot إلى المجموعة)
6. أدخل TOTP_SECRET الخاص بك
7. اختر الإعدادات المناسبة مثل فترة الإرسال والمنطقة الزمنية وصيغة الوقت

### لوحة تحكم المسؤول

تتيح لوحة تحكم المسؤول الوصول إلى الخيارات التالية:

#### 1. إضافة/تعديل مجموعة وإعداد TOTP_SECRET

يتيح هذا الخيار إضافة مجموعة جديدة أو تعديل مجموعة موجودة وتعيين TOTP_SECRET الخاص بها.

#### 2. تعديل فترة إرسال الرموز

يتيح هذا الخيار تعديل الفترة الزمنية بين إرسال الرموز (بالدقائق) لكل مجموعة.

#### 3. تخصيص شكل رسالة الرمز

يتيح هذا الخيار تخصيص شكل رسالة الرمز من خلال:
- تغيير المنطقة الزمنية (توقيت فلسطين أو التوقيت العالمي)
- تغيير صيغة الوقت (12 ساعة أو 24 ساعة)

#### 4. إدارة محاولات المستخدمين

يتيح هذا الخيار عرض وإدارة عدد محاولات النسخ المتبقية لكل مستخدم، ويمكن:
- عرض عدد المحاولات المتبقية لكل مستخدم
- إعادة تعيين محاولات مستخدم معين
- إعادة تعيين جميع محاولات المستخدم

## آلية عمل البوت

### توليد رموز المصادقة

يستخدم البوت مكتبة `pyotp` لتوليد رموز TOTP (Time-based One-Time Password) من TOTP_SECRET المقدم. يتم توليد رمز جديد كل فترة زمنية محددة (افتراضياً كل 10 دقائق).

### إرسال الرموز

يتم إرسال الرموز إلى المجموعات المحددة بشكل دوري حسب الفترة المحددة لكل مجموعة. تحتوي الرسالة على:
- عنوان: 🔐 2FA Verification Code
- زر "Copy Code" لنسخ الرمز
- وقت الرمز القادم بالصيغة المحددة

### نظام محاولات النسخ

يتتبع البوت عدد مرات نسخ الرمز لكل مستخدم، ويتم تحديد عدد المحاولات المسموحة لكل مستخدم (افتراضياً 3 محاولات). يتم إعادة تعيين المحاولات يومياً عند منتصف الليل.

### تعدد المجموعات

يدعم البوت العمل مع عدة مجموعات في نفس الوقت، حيث يمكن لكل مجموعة أن يكون لها:
- TOTP_SECRET خاص بها
- فترة إرسال خاصة
- منطقة زمنية خاصة
- صيغة وقت خاصة

## استكشاف الأخطاء وإصلاحها

### البوت لا يرسل الرموز

- تأكد من أن البوت لديه صلاحيات الإدارة في المجموعة
- تأكد من صحة معرف المجموعة
- تأكد من صحة TOTP_SECRET
- تحقق من سجلات البوت للبحث عن أي أخطاء

### خطأ في توليد الرموز

- تأكد من صحة TOTP_SECRET
- تأكد من تثبيت مكتبة `pyotp` بشكل صحيح

### البوت لا يستجيب للأوامر

- تأكد من أن البوت يعمل
- تأكد من أنك تستخدم الأمر الصحيح (`/admin`)
- تأكد من أن معرف المستخدم الخاص بك مضاف إلى قائمة المسؤولين

## تخصيص البوت

### تعديل قوالب الرسائل

يمكنك تعديل قوالب الرسائل في ملف `config.py`:

```python
MESSAGE_TEMPLATES = {
    "header": "🔐 2FA Verification Code",
    "footer": "Next code at: {next_time}",
    "welcome": "مرحباً بك في بوت المصادقة 2FA!\nاستخدم الأمر /admin للوصول إلى لوحة التحكم.",
    "admin_welcome": "مرحباً بك في لوحة تحكم المسؤول",
    "group_id_request": "الرجاء إدخال معرف المجموعة الخاصة:",
    "totp_secret_request": "الرجاء إدخال TOTP_SECRET:",
    "success": "تمت العملية بنجاح!",
    "error": "حدث خطأ: {error}",
    "attempts_left": "عدد المحاولات المتبقية: {attempts}",
    "copy_button": "Copy Code",
    "no_permission": "ليس لديك صلاحية لاستخدام هذا الأمر.",
}
```

### تعديل الإعدادات الافتراضية

يمكنك تعديل الإعدادات الافتراضية في ملف `config.py`:

```python
BOT_CONFIG = {
    "TOKEN": "YOUR_BOT_TOKEN",
    "ADMIN_IDS": ["YOUR_ADMIN_ID"],
    "DEFAULT_INTERVAL": 10,
    "DEFAULT_TIMEZONE": "Asia/Jerusalem",
    "DEFAULT_TIME_FORMAT": "12",
    "DEFAULT_ATTEMPTS": 3,
}
```

## الأمان

- تأكد من إضافة البوت إلى مجموعات خاصة فقط
- لا تشارك TOTP_SECRET مع أي شخص غير موثوق
- قم بتغيير رمز البوت (TOKEN) إذا تم تسريبه
- قم بإدارة قائمة المسؤولين بعناية

## ملاحظات هامة

- يتم إعادة تعيين محاولات المستخدمين يومياً عند منتصف الليل
- يجب أن يكون للبوت صلاحيات الإدارة في المجموعة ليتمكن من إرسال الرسائل
- يمكن للمسؤولين فقط الوصول إلى لوحة التحكم
- يمكن للبوت العمل مع عدة مجموعات في نفس الوقت
